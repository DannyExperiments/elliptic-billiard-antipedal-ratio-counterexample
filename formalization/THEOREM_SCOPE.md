# Formalization scope boundary

## Integrated partial certificate

The audited Lean lane checked a concrete finite quotient-algebra certificate:

- quartic and semiaxis relations plus rational endpoint signs;
- eight distinct indexed coefficient points and their central symmetry;
- outer-conic and dual-caustic algebraic residuals;
- a squared-and-cleared reflection residual;
- all sixteen complete supporting-line incidences with determinant units;
- central-inversion focus exchange; and
- both exact signed double-areas equal to zero, with both nonzero-denominator
  predicates failing.

The build used Lean 4.30.0, a zero-package manifest, and only bundled modules.
Its axiom audit reported only `propext`, `Classical.choice`, and `Quot.sound`.

## Not formalized

- a real embedding or real-root existence, uniqueness, and isolation;
- positivity and open-segment caustic contact;
- the unsquared directed reflection law and sign choice;
- cyclic order, primitivity, and one-winding topology;
- a continuous billiard/Poncelet-family statement;
- literal half-rays;
- source interpretation, priority, or publication claims; and
- the full named-problem resolution.

The inspected files are integrated and rebuilt, and the private draft PR's
exact-head Lean workflow passes. No badge is authorized while the repository
remains private. After an explicitly authorized public transition and a green
public-`main` workflow, the maximum accurate description is “Partial finite
exact certificate (Lean),” not “Lean-verified counterexample.”
