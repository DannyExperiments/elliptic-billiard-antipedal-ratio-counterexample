# Release notes draft

No release exists. This placeholder is not a version announcement.

The first immutable release, if authorized, should identify the exact
supporting-line domain counterexample, corrected all-even signed identity,
bounded priority language, manuscript hash, public evidence hash, verifier
replay hash, formalization scope, and all known exclusions.

Do not assign a version or release date until the release assets are frozen.

